---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

params:
  CF_MANIFEST_HOST_VMWARE:

inputs:
  - name: pcfdemo
  - name: candidate-release
  - name: version

outputs:
  - name: prepare-wmware

run:
  path: pcfdemo/ci/tasks/prepare-wmware.sh
  args: [
    --input-dir, candidate-release,
    --output-dir, prepare-wmware,
    --version-file, version/number,
    --input-manifest, pcfdemo/manifest.yml,
    --artifactId, pcf-demo,
    --packaging, war
  ]
